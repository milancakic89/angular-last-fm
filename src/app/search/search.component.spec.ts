import { SearchService } from './search.service';

describe('SearchComponent', ()=>{
  let component: SearchService;

  beforeEach(()=>{
    component = new SearchService(null, null)
  })

  it('Should search something', ()=>{
    // something here
  })
})